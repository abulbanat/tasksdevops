apiVersion: v1
kind: Service
metadata:
  name: otrabotka-app-service
  labels:
    app: otrabotka-app
spec:
  type: NodePort
  selector:
    app: otrabotka-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
